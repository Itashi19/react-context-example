import { createContext } from "react";

const NoteContext = createContext();

//context basically storage for holding the state and which will provide access of the components to its child components

export default NoteContext;
